steps:
  - id: "retrieve the file"
    name: "python:3.7"
    dir: "scripts"
    entrypoint: "bash"
    args:
      - '-c'
      - |
        pip install -r requirements.txt
        python gcs.py --bucket=zhong-gcp

